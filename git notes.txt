git --version

git config --global user.name "Ranganath"

git config --global user.email "vadapalliranganath@gmail.com"

git config --list

git config user.name

git config user.email

pwd

cd <path_repo> 

ls

git init

ls -la

// add files

git add .

git commit -m "This is my first commit"

git log

git log --author="Ranganath"

//below command to check pending changes to commit 

git status

//supose if you are updating or adding multiple files then use below commands

git add sample.txt

git add .

git commit -m "give ur commnets"

working copy > staging area > repository

git commit -m "updating"


















